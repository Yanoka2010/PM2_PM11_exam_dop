<!DOCTYPE html>
<html>
<head>
    <title>Авторизация</title>
    <style>
        body { font-family: Times New Roman; background: #FFFFFF; margin: 0; padding: 20px; }
        .form { max-width: 300px; margin: 0 auto; text-align: left; }
        .label { display: block; margin: 10px 0 5px; }
        .input { width: 100%; padding: 5px; box-sizing: border-box; }
        .error { color: #FF0000; margin-top: 10px; }
        .buttons { margin-top: 20px; }
        .button { background: #00FA9A; border: none; padding: 10px 20px; margin-right: 10px; }
    </style>
</head>
<body>
    <h1>Авторизация</h1>
    <div class="form">
        <form method="POST" action="/login">
            <div class="label">Логин:</div>
            <input type="text" name="login" class="input"><br>
            <div class="label">Пароль:</div>
            <input type="password" name="password" class="input"><br>
            <div class="buttons">
                <button type="submit" class="button">Войти</button>
                <button type="button" class="button" onclick="window.location.href='/login?guest=true'">Гость</button>
            </div>
        </form>
        <% if (error) { %>
            <p class="error"><%= error %></p>
        <% } %>
    </div>
</body>
</html>